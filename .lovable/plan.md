## Plan

1. Update only the business dashboard currency formatter in `src/components/dashboard/lateen/business.script.js`.
2. Make Arabic business money output match the existing marketer-role formatter exactly: amount + currency symbol/code in the same inline HTML structure.
3. Do not change CSS, cards, galleries, scrolling, layout, database logic, or any other file.
4. Verify by searching the business script to confirm only the currency formatter line changed.